#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../mongodb-download/built/mongodb-download-cli.js" "$@"
else 
  exec node  "$basedir/../mongodb-download/built/mongodb-download-cli.js" "$@"
fi
